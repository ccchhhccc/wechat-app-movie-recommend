<!--pages/rankkey/rankkey.wxml-->
<view>
<text wx:for="{{obj}}" wx:key="{{index}}" data-id="{{item.key}}" bindtap='goToDetail' style="background-color:{{'rgba'+item.color}}"># {{item.title}}</text>
</view>

